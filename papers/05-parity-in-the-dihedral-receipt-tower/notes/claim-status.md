# Claim status

Closed:

- involutions `A,B` whose product `H=AB` has order `2n` generate the
  dihedral group of order `4n`;
- `H_inverse=BA`;
- `c=[A,B]=H^2` has order `n`;
- the commutator subgroup is `<c>=<H^2>`;
- the binary return receipt is `tau=H^n`;
- for even `n`, `tau=c^(n/2)`;
- for odd `n`, `tau` is not in `<c>` and `<H>=<c> x <tau>`;
- for odd `n`, `H=c^((n+1)/2) tau`;
- the natural Klein-four encounter quotient maps `tau` to the identity
  exactly when `n` is even;
- the `n=4` case recovers the receipt tower `c=H^2`, `tau=c^2=H^4`
  inside the dihedral group of order sixteen.

Open or intentionally excluded:

- derivation of the involutive factorization from physical structure;
- native selection of a particular visible cycle length;
- physical meaning of the commutator or return receipt;
- spatial, temporal, energetic, gravitational, radiative, or particle claims.

