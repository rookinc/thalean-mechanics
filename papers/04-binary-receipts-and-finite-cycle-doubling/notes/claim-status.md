# Claim status

Closed:

- deterministic binary lifts of a directed `n`-cycle are voltage assignments in `C_2`;
- circuit holonomy is invariant under fiber-preserving gauge;
- equal holonomy is sufficient for gauge equivalence;
- exactly two gauge classes occur;
- trivial holonomy gives `C_n` disjoint union `C_n`;
- nontrivial holonomy gives the connected cycle `C_{2n}`;
- a nontrivial full-circuit receipt forces lifted successor order `2n`;
- the connected lift is unique up to fiber-preserving gauge and isomorphism;
- the previously proved `C_4` to `C_8` result is the case `n=4`.

Open or intentionally excluded:

- derivation of a visible cycle from physical structure;
- derivation of a binary receipt fiber from physical structure;
- selection of nontrivial rather than trivial holonomy by native data;
- spatial, temporal, energetic, gravitational, radiative, or particle interpretations.

