# G60 native orientation-root reversal census 011e

## Result

The canonical native root set

`C_tau = {c in Aut(G60) : c^2 = tau}`

contains twenty order-four elements. They divide into ten distinct inverse
pairs `{c, inverse(c)}`.

All twenty roots form one full-automorphism conjugacy orbit. The ten
unordered inverse pairs also form one conjugacy orbit. Thus the raw
unoriented datum canonically selects the complete root family, but neither
one inverse pair nor one direction within a pair.

Every root has an involutive symmetry in Aut(G60) sending it to its
inverse. Across the complete census there are 320 root-reverser witnesses,
and every generated subgroup has order 8. These are dihedral reversal
completions.

This instantiates the orientation obstruction on the native order-four
orientation-readout layer. It does not relabel these roots as the absent
order-eight evolver H.

The frozen outcome label is `multiple_reversal_orbits`. Here that means
multiple inverse pairs; all ten pairs lie in one larger conjugacy orbit.

No orientation or minimal directional datum is selected. No larger carrier,
manuscript, geometry, or physics claim is introduced.
