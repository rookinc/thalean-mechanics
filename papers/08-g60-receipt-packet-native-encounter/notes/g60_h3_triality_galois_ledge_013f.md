# G60 H3 triality-Galois ledge 013f

## Earned theorem

Native G60 has three exact spherical presentations over
`Q(sqrt5)` on the oriented orbit of adjacent-root bisectors
of the H3 root system. Each presentation is explicitly
isomorphic to the native sixty-vertex G60 graph.

The three presentations form an `S3` triality on the same
oriented sixty-point carrier. Their three fixed-point-free
orientation-sheet involutions commute, multiply pairwise to
the third, and form `V4`. Triality acts on this `V4` by its
full automorphism group, extending it to `S4`.

The common presentation kernel is `A5 x V4`, of order 240.
Together with triality it lies inside an order-1440
`A5 semidirect S4` extension.

The cyclic triality acts internally on `A5`. The reversing
involution is outer: it exchanges the two conjugacy classes
of order-five elements. Exact field conjugation

`sqrt5 -> -sqrt5`

exchanges the first two spherical presentations and fixes
the third, exactly matching the reversing triality action.

## Exact geometric handle

The golden character exchange selects a rank-one
longitudinal direction. Over `Q(sqrt5)` this direction is
exactly the bisector of an adjacent H3 root pair with inner
product

`(1 + sqrt5) / 4`.

The oriented bisector orbit has sixty points over thirty
projective lines and supports all three native G60
presentations.

## Refined negative result

The early direction audit failed because it required cyclic
triality to commute with the antipodal map. The complete
census proves that this requirement is false for all 84
order-three triality realizations.

The correct statement is:

- involutive reversal preserves antipodal pairing;
- cyclic triality intrinsically mixes orientation sheets;
- directionality belongs to the oriented sixty-point
  carrier and does not descend to the thirty projective
  lines.

## Boundary

This theorem does not select an absolute axis sign, identify
physical space, derive literal physical motion, identify
G900, or make a physical claim. The manuscript is unchanged.

## Locked authorities

- exact derivation SHA256:
  `49363d7a4893416e02edad7ef1558fdc30f9f957eea0cb7043908c517b60e617`
- raw receipt SHA256:
  `ad4b1be4d6f7a5d7e61a7f31792dc119fbe0d1b0aed9b85ceff81c212b029272`
- consolidation script SHA256:
  `60b925d715c645df52f368bda442d7ab5bc102391552049d68afd415bd5d5d72`
- source candidate SHA256:
  `11b4e1b1b9ef2126fe61a9a0f43dde07e41505d3cadafc4e9149c893e3b83c58`
- independent guard SHA256:
  `1f9aee99f2b74163a758517a56108e0d24195dbf8a3b9225aac44aefc897f346`
- independent guard receipt SHA256:
  `284fec797694ac15179ca6632be010595c9da58e31c1cc4245041ee55edc9a20`
