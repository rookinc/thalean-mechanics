# G60 fourth-root structured absence anatomy 011d

## Exact absence

The full element-order profile of the native automorphism group is

`1:1, 2:83, 3:20, 4:140, 5:24, 6:100, 10:72, 12:40`.

There are no elements of order 8.

Nevertheless, the canonical central involution tau has exactly twenty
square roots:

`c^2 = tau`.

All twenty roots have order 4 and form one full-automorphism conjugacy
class. None of them has a native square root. Therefore no native H
satisfies

`H^2 = c`

or equivalently

`H^4 = tau`.

This is a two-stage structured absence. The first receipt doubling exists:

`C2 -> C4`.

The second evolver doubling does not exist inside Aut(G60):

`C4 -/-> C8`.

## Interior return

For every square root c of tau,

`c^2 = tau`

while in the quotient by Z1,

`(c Z1)^2 = Z1`.

The quotient-visible action returns after two steps, but the full state has
acquired the internal binary receipt. The return changes the fiber
coordinate while preserving the quotient observation.

The other two nonidentity members of Z2 have no square roots at all. Tau
is uniquely root-bearing inside the nontrivial Z2 layer.

## Observation and receipt are transverse

The full action is vertex-transitive. Every vertex stabilizer has order 8
with profile

`1 identity, 5 involutions, 2 elements of order 4`.

This identifies the stabilizer as the dihedral group D8 of order 8, not
the cyclic group C8.

Every vertex stabilizer intersects Z1 and Z2 only in the identity. Its
setwise product with Z2 has size 32. Thus observation-preserving isotropy
and fiber-changing receipt are transverse native directions.

The native group contains eightfold local symmetry, but not an eight-step
cyclic evolver.

## Boundary

No replacement selector was searched. No larger carrier was constructed.
The orientation obstruction remains conditional because no native H exists
on the 60-vertex automorphism carrier.

No manuscript, geometry, or physics claim is made.
