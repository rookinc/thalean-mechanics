# G60 center-quotient character bridge preregistration 011t

## Question

Although the 011q local side is not the 011o orientation sheet, does the 011o
character factor canonically through the center quotient of the selected D8
extension?

## Candidate bridge

Let E be either selected 011q D8 presentation and let z be its nonidentity
center. The candidate factorization is

E -> E/<z> = V4 -> C2.

The quotient removes the local side. The predicted descended character is

- lambda(1)=0
- lambda(a)=0
- lambda(b)=1
- lambda(ab)=1

Its kernel {1,a} is predicted to be the axis distinguished by the locked
quadratic signature q(a)=1, q(b)=q(ab)=0.

## Frozen finite prediction

The ten transpositions of S5 have forty full-group lifts. These are predicted
to partition into ten native D8 subgroups, four lifts per subgroup.

For each of the two selected cocycle presentations and each of the ten native
D8 subgroups, exactly eight isomorphisms are predicted. This gives 160
isomorphism-character comparisons.

Every pulled-back alpha_1 character is predicted to kill the local center and
descend to the same V4 character 0,0,1,1. The pulled-back kernel is predicted
to be C4.

Predicted classification:

`unique_center_quotient_character_bridge`

## Boundary

This quotient bridge forgets rather than identifies the local route side. It
does not reverse the 011s obstruction. It does not establish a native update
law, mechanics state cell, selected orientation, geometry, or physical claim.
