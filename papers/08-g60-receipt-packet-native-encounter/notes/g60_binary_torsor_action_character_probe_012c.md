# G60 binary torsor action-character probe 012c

## Status

This is an exact, independently guarded, non-preregistered comparison of
three previously exposed two-element structures.

It does not assume that matching cardinality implies canonical identity.

## Instruction and chart characters

For both selected D8 presentations, the eight local automorphisms act on:

- the q-axis order-four instruction pair `[2,3]`;
- the two global forty-chart orbit classes.

The instruction character has kernel indices:

`[0,1,2,3]`

The chart character has kernel indices:

`[0,1,4,5]`

Their kernel intersection is:

`[0,1]`

Their joint image is the full product:

`[(0,0),(0,1),(1,0),(1,1)]`

Therefore the instruction and chart torsors carry independent nontrivial
C2 characters of local `Aut(D8)`. They are not equivariantly identical.

## Presentation gauge obstruction

Across the four locked presentation gauge maps:

- instruction gauge bits are `[0,0,1,1]`;
- chart gauge bits are `[0,0,0,0]`;
- comparison deltas contain both `0` and `1`.

Therefore no presentation-gauge-compatible family of bijections exists
between the instruction and chart torsors.

## Orientation torsor

The 011o orientation bridge set has two elements. Source-sheet reversal
and root inversion both exchange them. Without an anchor, both remain.
Each of the forty compatible anchors selects exactly one bridge.

No common acting-group map from this reversal action to local `Aut(D8)`
has been established. Therefore no identification with either the
instruction torsor or chart torsor is earned, and the orientation anchor
cannot yet be transferred to either choice.

## Boundary

This packet does not establish:

- a three-way canonical torsor identification;
- an orientation-to-`Aut(D8)` action bridge;
- anchor transfer;
- an autonomous update instruction;
- an absolute chart orbit;
- an unanchored orientation;
- a mechanics state cell;
- geometry;
- a physical claim.

The manuscript and polish pile remain untouched.

## Provenance

- Candidate script SHA256: `fb7e5caa8d09110a81ff016302c976cd146eb1baf178dc0ba0e45f1b852bef2a`
- Candidate report SHA256: `868d89956ca8a2af5911334197ab5c5be92989c5d3689367a16b0e9c074d3e96`
- Memory-safe guard SHA256: `0caf690af0a5b0c5af70ff29c67ce5f97a193ec7f613cf8a7dacc99c4002e917`
- Guard report SHA256: `d5e629b06f13930ebb13a74b09e2e0f3900712cb79e34c801f1c8d054061ece7`
- Guard checks: `63`
- Guard failures: `0`
