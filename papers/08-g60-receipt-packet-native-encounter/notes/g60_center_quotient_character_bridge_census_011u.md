# G60 center-quotient character bridge census 011u

## Status

Frozen complete marked comparison census.

The result was computed after preregistration 011t at locked head:

`9aafeab Preregister G60 center-quotient character bridge`

## Result

The full 480-element native automorphism group contains forty elements
whose five-point image is a transposition. Together with the native V4,
these lifts generate exactly ten distinct native D8 subgroups. Each
subgroup contains four transposition lifts.

The selected 011q central extension has two gauge-related normalized
cocycle presentations. For every presentation and every native D8
subgroup there are exactly eight group isomorphisms. This gives twenty
presentation-subgroup pairs and 160 marked comparisons.

Every isomorphism maps the local central flip to the unique nonidentity
native center a. The locked 011o alpha_1 character kills this center.
Consequently every pulled-back character descends through the center
quotient to the visible V4 register.

All 160 comparisons produce the same descended character:

- lambda(1) = 0
- lambda(a) = 0
- lambda(b) = 1
- lambda(ab) = 1

Its quotient kernel is exactly {1,a}, the axis distinguished by the
locked quadratic signature q(a)=1, q(b)=q(ab)=0. Its full preimage in
the local D8 extension is C4. Both gauge-related cocycle presentations
produce the same character data.

The classification is:

`unique_center_quotient_character_bridge`

## Interpretation

The local two-sided history witness and the full-A orientation
character meet only after the local central side is forgotten:

`E -> E/<z> ~= V4 -> C2`

This is a positive center-quotient bridge. It does not reverse the 011s
obstruction. The absolute local side is still not the 011o orientation
sheet.

## Corrected character identification

A preserved preliminary run used `p xor d8_flip`. Independent hash
checking showed that this reconstructs the locked character `n`, not
the locked character `alpha_1`.

The corrected computation uses:

`alpha_1 = d8_flip`

Its complete 480-value hash exactly matches the locked 011o alpha_1
hash.

The incorrect run remains recorded by hash and was not promoted.

## Boundary

This packet does not:

- identify the local central side with the 011o orientation sheet
- select an absolute side or orientation
- construct a native update law
- establish a mechanics state cell
- prove global minimality
- mutate the manuscript
- make a geometry or physical claim
