# G900 native K3,3 cube-affine capstone 062

Audit pass: true

Classification:

`native_G15_K3_3_signed_axis_cube_projection_affine_twist_midpoint_quotient_capstone_passed`

## Native G15 bridge

The fifteen standard Petersen-edge bisectors map bijectively to
native G15 and preserve all thirty line-graph adjacencies.

The projected six-point K3,3 frame is not an adjacency-faithful
subgraph of G15. It is instead an exact distance-role projection
with profile:

- six roles at native distance 1;
- two roles at native distance 2;
- one role at native distance 3.

## Five-edge register algebra

Each native spoke is the common tip of one inner-pentagram
triangle and one outer-pentagon triangle. The five spokes induce
a bijection between the two C5 edge registers.

In oriented coordinates this bridge is affine with multiplier 2
or its inverse 3 modulo 5. Every coordinate gauge gives an
order-four permutation with one fixed role and one four-cycle.

Together with C5 rotation, the induced register algebra is

`AGL(1,5) = C5 semidirect C4`

of order 20.

The two oriented order-four twists have the same square:

`x -> -x mod 5`

with role profile `1 + 2 + 2`.

## Registered angle placement

The graph does not derive the numerical angle multiset.

Given the externally registered multiset consisting of one 180
and four 90 values, twist invariance uniquely places 180 on the
fixed bridge and 90 on the four-cycle.

## Midpoint quotient

Before quotienting, the five native spokes correspond
bijectively to the five formal inner-edge midpoints. Both
directions exist.

After collapsing the five midpoint sections to one quotient
point Y1, forward descent remains defined. The reverse relation
has a five-element fiber and no C5-equivariant section.

Thus the undefined reverse is a quotient-section obstruction,
not a failure of native spoke-midpoint invertibility.

## K3,3 projection counts

The projected carrier has:

- six K3,3 vertices;
- nine K3,3 edges;
- five interior edges;
- four boundary edges.

Its barycentric shadow has six original vertices plus nine edge
midpoints, for fifteen roles. This cardinality matches G15, but
the barycentric graph is not graph-isomorphic to native G15.

## Signed-axis cube-face realization

The six K3,3 vertices admit a signed-axis cube-face realization.

The six cube-adjacent cross relations are exactly the six native
distance-1 roles.

The three opposite-face relations are:

- AE at native distance 2;
- BC at native distance 2;
- DF at native distance 3.

Reflection reduces 72 initial cube-face labelings to 24.
The native distance filter reduces these to 12.

Those twelve placements form one free transitive residual gauge
orbit with group:

`S3 x C2`

The opposite pairs are AE, BC, and FD.

## Boundary

This is a post-scout capstone consolidation, not a blinded
preregistered discovery.

No adjacency-faithful K3,3 embedding in G15 is claimed.

No native G15 automorphism group identification with AGL(1,5)
is claimed. AGL(1,5) is the induced five-position register
algebra.

No absolute axis names, global sign, handedness, canonical cube
placement, physical cube, physical distance, physical rotation,
force, gravity, or other physical claim is made.
