# G900 intrinsic six-register synthematic capstone 073

## Result

Native G15 intrinsically determines six double-C5 decompositions corresponding
to the six perfect matchings of its Petersen root. Pairwise intersections
identify the fifteen G15 states with the fifteen duads of a six-point register.

A native synthematic total reconstructs the complete G15 metric:

- distance 1 means disjoint duads in different synthemes;
- distance 2 means duads intersect in one register point;
- distance 3 means disjoint duads in the same syntheme.

The ten unordered three-plus-three splits of the six-point register produce ten
intrinsic abstract K3,3 cross-duad carriers. Each split partitions the fifteen
native states into six same-side duads and nine cross-duads.

Adding one same-side closure gives sixty reflection frames. The proposed single
120-element frame torsor is false. The 120 complete five-four placements instead
split into two native orbits of sixty. Each base frame has one placement on each
sheet.

The sheet bit is exactly the choice between the two reflection-invariant
synthematic-total completions of the fixed syntheme AB, CE, DF.

Each base frame also has two inverse centered order-four square roots of its
reflection. Both roots preserve both sheets. Completion sheet and affine-root
orientation are therefore independent binary choices, giving four combined
choices per base frame.

## Orthogonal language

The native graph derives an order-four step and an involutive repeated step.

Under a separately chosen faithful square-grid or inner-product realization,
these roles may be called:

- orthogonal once;
- orthogonal again.

This language is conditional on that realization. The graph does not by itself
derive numerical 90-degree or 180-degree angles.

## Preserved falsifier

Packet 070 is intentionally retained. It falsifies the claim that all 120
complete five-four frames form one free transitive native torsor. Its three
failed checks are expected and are corrected by packet 070b's two-sheet result.

## Boundaries

No canonical decomposition, K3,3 split, frame, completion sheet, or affine
orientation is selected. No unconditional metric orthogonality, numerical-angle,
physical-cube, physical-distance, physical-rotation, force, or gravity claim is
made.
