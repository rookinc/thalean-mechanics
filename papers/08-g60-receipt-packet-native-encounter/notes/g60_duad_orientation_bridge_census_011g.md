# G60 duad-orientation bridge census 011g

## Result

The preregistered structural prediction passed exactly.

The canonical five-matching action supplies one transitive set of twenty
ordered duads and one transitive set of ten unordered duads. Conjugation
supplies one transitive set of twenty roots of tau and one transitive set of
ten inverse-root pairs.

There is exactly one Aut(G60)-equivariant bijection from unordered duads to
inverse-root pairs. There is no equivariant bijection from ordered duads to
individual roots.

## Kernel obstruction

The pointwise kernels are:

- ordered duads: `[0,65,124,326] = Z2(A)`;
- unordered duads: `[0,65,124,326] = Z2(A)`;
- individual roots: `[0,326] = Z1(A)`;
- inverse-root pairs: `[0,65,124,326] = Z2(A)`.

Elements 65 and 124 fix all visible duads and all inverse-root pairs, but send
every root to its inverse. Elements 0 and 326 fix every root under conjugation.

Therefore the orientation-root family is a two-state interior lift of the
visible inverse-pair family, with residual deck action

`Z2(A)/Z1(A) isomorphic C2`.

## Earned boundary

The raw graph canonically selects one reversal-covariant inverse pair through
the five-matching quotient. It does not select either orientation inside that
pair. Ordering a visible duad does not remove the obstruction because the
native V4 kernel acts trivially on the ordered-duad action.

No minimal additional directional datum has yet been identified. No larger
evolver carrier, geometry claim, manuscript change, or physical claim is
made.
