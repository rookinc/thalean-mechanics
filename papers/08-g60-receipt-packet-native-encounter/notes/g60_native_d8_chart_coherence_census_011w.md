# G60 native D8 chart-coherence census 011w

## Status

Frozen complete chart-orbit census.

The census was performed after preregistration 011v at locked head:

`f43e86b Preregister G60 native D8 chart coherence test`

## Result

The two gauge-related presentations of the selected 011q D8 extension
each supply eighty isomorphism charts over the ten native
transposition-local D8 subgroups.

The full automorphism group acts validly on both chart bundles by
postcomposition with conjugation. Identity and closure failure counts
are zero.

The ten native D8 subgroups form one base orbit. Each base stabilizer
has order 48.

For each cocycle presentation:

- the eighty charts split into two orbits of size forty
- every chart stabilizer has order 12
- each eight-chart fiber splits into two orbits of size four
- the normalizer image on the fiber has order four
- that image is exactly the inner automorphism group of D8
- no strict equivariant one-chart-per-subgroup section exists

The residual chart gauge is therefore:

`Out(D8) = C2`

There are four normalized gauge isomorphisms between the two cocycle
presentations. Every induced chart bijection intertwines the full-group
actions and maps each forty-chart orbit to exactly one forty-chart
orbit. The two presentations therefore define equivalent chart
bundles.

The locked alpha_1 character and q-axis signature are constant across
both outer chart orbits. They do not select one orbit.

The classification is:

`native_D8_chart_bundle_has_exact_outer_C2_obstruction_to_equivariant_section`

## Interpretation

The native local system is gauge-covariant. It supplies a coherent
bundle of local D8 update charts, but the native data does not choose
an absolute chart. A residual outer C2 gauge remains.

This classifies the obstruction precisely. It does not construct an
absolute native update law or a mechanics state cell.

## Boundary

This packet does not:

- select a strict equivariant chart
- identify the local central side with the 011o orientation sheet
- select an absolute side or orientation
- construct a native update law
- establish a mechanics state cell
- prove global minimality
- mutate the manuscript
- make a geometry or physical claim
