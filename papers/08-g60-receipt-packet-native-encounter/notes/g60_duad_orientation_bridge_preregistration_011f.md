# G60 duad-orientation bridge preregistration 011f

## Status

This is a structural preregistration written after inspecting the schemas of
certificates 077, 078, 079, and Packet 011e, but before computing any
duad-to-root equivariant correspondence.

It is not blind. The inspected authorities already suggest a kernel mismatch,
so that prediction is recorded explicitly rather than concealed.

## Frozen objects

The canonical five matching blocks define:

- 20 ordered duads `(i,j)` with `i != j`;
- 10 unordered duads `{i,j}`.

The canonical central involution defines:

- 20 orientation roots `C_tau={c:c^2=tau}`;
- 10 inverse pairs `{c,c_inverse}`.

The full native automorphism group acts on the duads through certificate 078
and on roots through conjugation.

## Structural prediction

The predicted result is:

- exactly one equivariant bridge from unordered duads to inverse-root pairs;
- no equivariant bridge from ordered duads to individual roots.

The proposed mechanism is that native V4 is invisible on the five-point
quotient but exchanges the two roots inside an inverse pair. If so, the
quotient selects the pair while the interior kernel prevents orientation.

This prediction is not a theorem and must survive the complete finite action
test.

## Boundary

No orientation is selected. Ordering a visible duad is not yet accepted as
directional data. No minimal directional datum has been identified. No larger
evolver carrier is constructed. No manuscript, geometry, or physical claim is
made.
