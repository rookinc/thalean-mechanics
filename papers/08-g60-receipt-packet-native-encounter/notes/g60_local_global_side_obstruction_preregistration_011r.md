# G60 local/global side obstruction preregistration 011r

## Question

Can the central side of the selected 011q two-sided slider be identified
directly with the 011o orientation sheet?

## Locked marked structures

The selected 011q cohomology class is D8 and has two gauge-related normalized
cocycle representatives. Its nonidentity central kernel element exchanges the
two local sides.

The native V4 elements have exact group indices:

- 1 = 0
- a = 326
- b = 124
- ab = 65

The 011o character alpha_1=p+n restricts to native V4 as:

- alpha_1(1)=0
- alpha_1(a)=0
- alpha_1(b)=1
- alpha_1(ab)=1

The native D8 center is the subgroup {1,a}. Therefore every abstract D8
isomorphism must send the selected extension's nonidentity central flip to
native a.

## Frozen prediction

For each of the two selected cocycle representatives, exactly eight group
isomorphisms to the native local D8 will be found, for sixteen total. Every
isomorphism will map the local central flip to native a.

The local central flip changes the local side by one. Native a changes the 011o
sheet by zero. Therefore no direct marked side identification will survive the
center-action test.

Predicted classification:

`native_center_kernel_obstructs_direct_side_identification`

## Boundary

This is a bounded marked-D8 comparison. Failure of direct identification does
not rule out a broader relation mediated by another object. No intermediate
bridge is constructed here. No local/global sheet identity, native update law,
mechanics state cell, orientation, geometry, or physical claim is made.
