# G60 native evolver candidate preregistration 011b

## Authority boundary

The locked sources do not contain an exact native evolver permutation.

Paper 01 supplies the abstract order-eight lift grammar. Paper 02 supplies
the abstract dihedral grammar `H=AB`, `inverse(H)=BA`. Packet 010c supplies
the exact native group A and its characteristic layers Z1 and Z2.

The Paper 03 orientation artifact is untracked. It is advisory only and is
excluded from the native candidate definition.

## Frozen native candidate

Let tau be the unique nonidentity element of Z1. Let the outer pair be
`Z2 minus Z1`.

The primary family U2 consists of all native automorphisms H satisfying:

1. H has exact order 8.
2. `H^4 = tau`.
3. Conjugation by H exchanges the two elements of `Z2 minus Z1`.
4. The cyclic subgroup generated by H intersects Z2 exactly in Z1.

The inverse partner is `inverse(H)`.

U0 removes conditions 3 and 4. U1 keeps condition 3 but removes condition
4. Their counts are ablation diagnostics only. Neither may replace U2
after the result is inspected.

## Reversal witness

For every U2 element, the census will search all involutions S in A such
that

`S*H*S = inverse(H)`.

It will also compute the order of the subgroup generated by H and S.
Because Z1 and Z2 are characteristic, conjugation inside A preserves the
base datum used by the orientation obstruction.

## Boundary

No native element has been counted or selected. U2 may be empty, may
contain one inverse pair, or may contain multiple inverse pairs. Every
outcome must be preserved.

No fallback selector, manuscript change, geometry claim, or physics claim
is allowed.
